<?xml version="1.0" encoding="utf-8"?><!--
   Bookline stores everything on-device only (see privacy policy).
   Exclude all app data from cloud backups and device-to-device
   transfers; the Keystore-encrypted prefs could not be decrypted
   after a restore anyway. allowBackup="false" covers Android < 12.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
